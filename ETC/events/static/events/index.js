function addEventManager() {
    // Get elements
    const button = document.querySelector('button#add-event-manager-button')
    const container = document.querySelector('div#add-event-manager-form-container')

    // Switch out element display
    button.style.display = 'none'
    container.style.display = 'block'
}

function addTeamMember() {
    // Get elements
    const button = document.querySelector('button#add-team-member-button')
    const container = document.querySelector('div#add-team-member-form-container')

    // Switch out element display
    button.style.display = 'none'
    container.style.display = 'block'
}